#
# wm independent hotkeys
#

# terminal emulator
#super + Return
#   alacritty

# run file manager
super + n
   pcmanfm

# launch browser
super + b
    chromium
# launche qutebrowser
super + shift + b
    qutebrowser
# program launcher
super + p
	drun
super + shift + p
    dmenu_run -nb black -i -fn 'Ubuntu-14'
# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd

#Volume Control
#XF86AudioRaiseVolume
#    pactl set-sink-volume 0 +5%
#XF86AudioLowerVolume
#    pactl set-sink-volume 0 -5%
#XF86AudioMute
#    pactl set-sink-mute 0 toggle

